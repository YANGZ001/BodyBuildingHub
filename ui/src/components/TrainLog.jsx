/* eslint "react/prefer-stateless-function": "off" */

import React from 'react';

export default class TrainLog extends React.Component {
  render() {
    return (
      <div>This is a placeholder for TrainLog component.</div>
    );
  }
}
